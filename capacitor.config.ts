import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'test-capacitor-issue-8324-android',
  webDir: 'dist'
};

export default config;
